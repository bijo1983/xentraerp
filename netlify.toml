[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18.20.8"
  NPM_CONFIG_PRODUCTION = "false"   # ⬅️ make sure devDependencies (vite) are installed

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
